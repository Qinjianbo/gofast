services:
    app:
        build:
            context: .
            dockerfile: Dockerfile.v8
            target: final
        command: ["/app/server", "--bind", "0.0.0.0:8080"]
        ports:
            - "8080:8080"
